<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="colorPrimaryDark">#000000</item>
        <item name="android:textViewStyle">@style/KoolewTextViewStyle</item>
        <item name="android:buttonStyle">@style/KoolewButtonStyle</item>
    </style>

    <style name="KoolewTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">sans－noto</item>
    </style>

    <style name="KoolewButtonStyle" parent="android:Widget.Holo.Button">
        <item name="android:fontFamily">sans-serif-light</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
